# RPA Core 依赖
# Python >= 3.8

# 浏览器自动化核心库
DrissionPage>=4.0.0

# HTTP API 服务
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# WebSocket 支持 (uvicorn 依赖此库处理 WebSocket 连接)
websockets>=12.0

# 凭据保险库的认证加密 (Fernet / AES)
cryptography>=42.0.0

# AI 视觉兜底定位 / 选择器修复（可选，仅在开启 RPA_AI_FALLBACK 时需要）
anthropic>=0.40
